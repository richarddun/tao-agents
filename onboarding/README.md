# Placeholder — add induction workflow here
#
# The induction workflow interviews the user about their environment,
# observes what it can safely inspect, proposes bindings and authority
# rules, confirms with explicit approval, validates readiness, and
# activates without modifying the curated core.
#
# Induction must never request credentials in conversational text
# or embed secrets in generated configuration.