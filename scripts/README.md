# Placeholder — add deterministic scripts here
#
# Scripts perform collection, validation, transformation, and controlled execution.
# They should be idempotent where practical, emit machine-readable results,
# fail loudly, and support dry-run modes for mutation operations.